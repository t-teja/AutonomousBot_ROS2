# Calibration service request
string component_name     # Component to calibrate (e.g., "imu", "motors", "steering")
bool reset_calibration   # Reset existing calibration data
float64 timeout          # Calibration timeout in seconds
---
# Calibration service response
bool success             # Calibration successful
string message           # Status message
float64 calibration_time # Time taken for calibration
string calibration_data  # Calibration parameters (JSON format)
